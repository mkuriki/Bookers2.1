  <% if @group.errors.any? %>
       <%= @group.errors.count %>errors prohibited this obj from being saved: 
       <ul>
       <% @group.errors.full_messages.each do |message| %>
       <li><%= message %></li>
       <% end %>
       </ul>
  <% end %> 

<body>
    <main>
        <p id = notice></p>
        <div class = container>
            <div class = row>
                <div class = "col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
                    <h1>Editing Group</h1>
                    <%= form_with model: @group do |f| %>
                      <div class="form-group">
                      <label for="group_name">Name</label>
                      <%= f.text_field :name, autofocus: true, placeholder: @group.name, class: "form-control group_title" %>
                      </div>
                      <div class = "form-group">
                      <label for="group_opinion">Introduction</label>
                      <%= f.text_area :body, autofocus: true, class: "form-control group_body" %>
                      </div>
                      <div class = "form-group">
                      <%= f.submit "Update group", class: "btn btn-success"  %>
                      </div>
                      <%= link_to "Show", group_path(@group) %>
                      <%= "|" %>
                      <%= link_to "Back", groups_path %>
                    <% end %>
                </div>
            </div>
        </div>
    </main>
</body>